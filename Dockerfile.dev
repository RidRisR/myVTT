FROM node:22-slim
WORKDIR /app
RUN npm install -g nodemon
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
EXPOSE 4444 5173
CMD nodemon --watch server/ --ext ts,mjs,js,json --exec "npx tsx server/index.ts" & npx vite --host 0.0.0.0 && wait
