FROM node:22-slim
WORKDIR /app
RUN npm install -g nodemon
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
EXPOSE 4444 5173
CMD nodemon --watch server/ --ext mjs,js,json server/index.mjs & npx vite --host 0.0.0.0 && wait
